//- auth layout
include ../config/config
block config
doctype html
html(lang="en")
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1, shrink-to-fit=no')
    meta(name='description' content='')
    meta(name='author' content='')
    title #{siteName} | #{pageName}

    // Favicon
    link(rel='apple-touch-icon' sizes='180x180' href=`${assets}img/favicon/apple-touch-icon.png`)
    link(rel='icon' type='image/png' sizes='32x32' href=`${assets}img/favicon/favicon-32x32.png`)
    link(rel='icon' type='image/png' sizes='16x16' href=`${assets}img/favicon/favicon-16x16.png`)

    // Css
    link(rel="stylesheet" href=`${assets}plugins/bootstrapicons/bootstrapicons.css` media="print" onload="this.media='all'")
    link(rel="stylesheet" href=`${assets}css/piruui.css`)
    link(rel="stylesheet" href=`${assets}plugins/extra/extra.css`)

    script.
      (function () {
        const storedTheme = localStorage.getItem('theme');
        const prefersDarkScheme = window.matchMedia('(prefers-color-scheme: dark)').matches;
        const theme = storedTheme || (prefersDarkScheme ? 'dark' : 'light');
        document.documentElement.setAttribute('data-bs-theme', theme);
      })();

    block styles
  body
    .piru-wrapper
      //- Floating Theme Toggle
      .position-fixed.top-0.end-0.p-4(style="z-index: 1050;")
        button#bd-theme-toggle.btn.btn-link.text-body.p-2.text-decoration-none(type="button" title="Toggle Theme")
            span.theme-icon-active
              i.bi.bi-sun.h4.mb-0

      main.piru-main-content.d-flex.align-items-center.justify-content-center.min-vh-100.p-3
        block content

    // Js Bootstrap
    script(src=`${assets}js/piruui.js`)

    block scripts 
